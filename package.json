{
  "name": "poker_checker",
  "version": "1.0.0",
  "description": "this script receives two arguments from CLI and decide which is better based on poker rules",
  "main": "/dist/poker.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "ts-node poker.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Iicytower/poker_checker.git"
  },
  "author": "Iicytower",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.21",
    "typescript": "^4.6.2",
    "ts-node": "^10.7.0"
  }
}
